name: Deploy to Hostinger (rayllarifas.site)

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      # Gera dist/ (site estático). O public/.htaccess do projeto já cuida do
      # roteamento SPA e é copiado automaticamente para dist/ pelo Vite.

      - name: Deploy via FTP para Hostinger
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          local-dir: ./dist/
          # Confirmado: esta conta FTP (u386043061.rayllarifas.site) ja abre
          # direto na raiz publica do dominio (nao tem subpasta public_html).
          server-dir: ./
          exclude: |
            **/.git*
            **/.git*/**
            **/.env
